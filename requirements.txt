# Core
openai>=1.40.0
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0
langgraph>=0.4.0

# Observability — pin to v3 (v4 has breaking changes; v2 is missing
# langfuse.langchain). All scripts in this repo use the v3 API.
langfuse>=3,<4

# Vector DB — fastembed extra enables client.query(query_text=...)
qdrant-client[fastembed]>=1.12.0
langchain-qdrant>=0.2.0

# HuggingFace datasets — required by ragas EvaluationDataset
datasets>=3.0.0

# RAG Evaluation
ragas>=0.2.0
deepeval>=1.0.0

# MLOps
mlflow>=2.17.0
scikit-learn>=1.5.0
dagster>=1.9.0
dagster-webserver>=1.9.0

# Agent Memory
mem0ai>=0.1.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0
pandas>=2.2.0
numpy>=1.26.0

# NeMo Guardrails
nemoguardrails>=0.10.0
